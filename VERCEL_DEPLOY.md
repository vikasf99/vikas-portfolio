# 🚀 Vercel Deployment - Quick Guide

Your code is now on GitHub at: https://github.com/vikasf99/vikas-portfolio

## Deploy to Vercel (3 Easy Steps)

### Step 1: Go to Vercel
Visit: https://vercel.com/new

### Step 2: Import Repository
1. Sign in with GitHub (if not already)
2. Click "Import" next to `vikasf99/vikas-portfolio`
3. Or click "Add New..." → "Project" → Select your repository

### Step 3: Deploy
1. **Framework Preset**: Next.js (auto-detected ✅)
2. **Root Directory**: `./` (default - leave as is)
3. **Build Command**: `npm run build` (default - leave as is)
4. **Output Directory**: `.next` (default - leave as is)
5. Click **"Deploy"**

That's it! Your site will be live in 1-2 minutes! 🎉

---

## Your Live URL

After deployment, you'll get a URL like:
- `https://vikas-portfolio.vercel.app`
- Or `https://vikas-portfolio-[random].vercel.app`

You can customize it in Vercel settings.

---

## Automatic Deployments

✅ **Every push to `main` branch** → Auto-deploys to production
✅ **Every push to other branches** → Creates preview deployment
✅ **Pull Requests** → Creates preview URL for testing

---

## Next Steps (Optional)

1. **Custom Domain**: 
   - Vercel Dashboard → Settings → Domains
   - Add your domain (e.g., `vikasshetty.com`)

2. **Environment Variables** (if needed later):
   - Vercel Dashboard → Settings → Environment Variables

3. **Analytics** (optional):
   - Enable Vercel Analytics in project settings

---

## Troubleshooting

If build fails:
1. Check build logs in Vercel dashboard
2. Test locally: `npm run build`
3. Ensure all dependencies are in `package.json`

Your portfolio is ready to go live! 🚀
